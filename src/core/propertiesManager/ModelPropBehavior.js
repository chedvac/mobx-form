export default class ModelPropBehavior {
  setReset = function(reset) {
    this.reset = reset;
  };
  setMap = function(map) {
    this.map = map;
  };
  setRef = function(ref) {
    this.ref = ref;
  };
}
