export default function getDomain(serviceType, serverName) {
  //   const domain =
  //     domains[serviceType][serverName || generalAttributes.getTargetServerName()];
  //   if (!domain) {
  //     exceptions.throwFormError(messages.invalidDomain);
  //   }
  // return domain;
  return 'http://formsdev.vcloud.gov.il/govservicelist/';
}
